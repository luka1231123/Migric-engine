#!/bin/sh

# Run the minimal engine compiled by build.sh.  This script
# launches the SFML window and enters the main loop.  The engine
# terminates when the user presses Escape or closes the window.
./engine